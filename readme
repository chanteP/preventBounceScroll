##移动设备web开发防滚插件

向上找到能滚的元素然后让它滚。因为太麻烦所以只写了纵向滚，<del>overflow-x是什么能吃？</del>。

判断条件:
1. <em style="color:#E45212;">getComputedStyle(element).overflow</em> 或者 <em style="color:#E45212;">getComputedStyle(element).overflowY</em> 的值为 <em style="color:#E45212;">scroll</em> 或者 <em style="color:#E45212;">auto</em>
2. 向上滚动并且能滚
3. 向下滚动并且能滚
4. 自己看代码

config:
1. <em style="color:#E45212;">isExtraElement</em> 排除节点判定
2. <em style="color:#E45212;">outerBounce</em> 外层滚动回弹，false为顶部、底部滚动时固定


    require('np-preventBounceScroll')
        .config({
            //config xxx
        })
        .bind()
        //把对应selector的节点移动到body里面
        .move(document.querySelector('[data-position="fixed"]'));